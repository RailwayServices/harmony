services:
  postgres:
    image: postgres:18-alpine
    container_name: harmony-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=waris
      - POSTGRES_DB=postgresx
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - "5432:5432"
    volumes:
      - pgdata18:/var/lib/postgresql

  redis:
    image: redis:8.8.0-alpine
    container_name: harmony-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

  bot:
    image: ghcr.io/harmonyservices/harmony:latest
    container_name: harmony-bot
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      - DATABASE_URL=postgresql://waris@postgres:5432/postgresx
      - REDIS_URL=redis://redis:6379
    depends_on:
      - postgres
      - redis

  api:
    image: ghcr.io/harmonyservices/harmony:latest
    container_name: harmony-api
    command: ["harmony-api"]
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ../.env
    environment:
      - DATABASE_URL=postgresql://waris@postgres:5432/postgresx
      - REDIS_URL=redis://redis:6379
      - API_PORT=3000
    depends_on:
      - postgres
      - redis

volumes:
  pgdata18:
  redisdata:
