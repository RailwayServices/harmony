[package]
name = "harmony-messaging"
version = "0.1.0"
edition = "2021"

[dependencies]
harmony-common = { path = "../common" }
tokio = { version = "1.52", features = ["sync"] }
thiserror = "2"
tracing = "0.1"
